# Live build — canonical Render backend
# Keep this in sync with src/config/apiBaseUrl.js so REST, sockets, uploads,
# and live updates all use the same healthy deployment.
VITE_API_URL=https://lms-adeeb-technology-lab.onrender.com/api
