# Live build — canonical Hostinger backend
# Keep this in sync with src/config/apiBaseUrl.js so REST, sockets, uploads,
# and live updates all use the same deployment.
VITE_API_URL=https://darksalmon-grasshopper-335002.hostingersite.com/api
